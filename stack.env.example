# Copiar este archivo a stack.env y configurar los valores
# NO subir stack.env al repositorio

JWT_SECRET=cambiar-por-un-secreto-largo-y-seguro
ADMIN_USER=usuario
ADMIN_PASSWORD_HASH=$2b$10$hash-generado-con-bcrypt

# Para generar el hash de la contraseña:
# node -e "console.log(require('bcryptjs').hashSync('tu-contraseña', 10))"
